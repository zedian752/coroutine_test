基于linux的ucontext和c语言的协程切换


1. 假设只有2条线程，线程A接收代码输入线程，线程B为协程主线程

 0 线程A接收到代码输入后，唤醒线程B，
 1 await read(fd, buffer, buffer_size)
 2 返回主协程，通过FIFO队列调度其他协程任务
 3 其他任务执行完之后又再次返回主协程，检查是否有微任务在队列中，有则执行微任务
 4 等待宏任务的进来，


 2 通过协程改造read, write等阻塞api
 同步阻塞怎么解决？？？？？？？？？？
